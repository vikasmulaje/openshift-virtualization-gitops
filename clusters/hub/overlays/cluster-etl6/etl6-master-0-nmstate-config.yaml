apiVersion: agent-install.openshift.io/v1beta1
kind: NMStateConfig
metadata:
  name: etl6-master-0
  namespace: etl6
  labels:
    infraenvs.agent-install.openshift.io: etl6
spec:
  config:
    interfaces:
    - name: enp1s0
      type: ethernet
      state: up
      ipv4:
        enabled: true
        address:
          - ip: ${NETWORK_SUBNET}.170
            prefix-length: 24
        dhcp: false
    dns-resolver:
      config:
        server:
        - ${NETWORK_SUBNET}.1
    routes:
      config:
      - destination: 0.0.0.0/0
        next-hop-address: ${NETWORK_SUBNET}.1
        next-hop-interface: enp1s0
  interfaces:
    - name: enp1s0
      macAddress: 52:54:00:aa:06:00
